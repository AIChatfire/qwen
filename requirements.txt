# 运行期依赖 —— 刻意保持最小面。
fastapi>=0.115
uvicorn>=0.30
gunicorn>=22.0
# gunicorn 的 ASGI worker。**必须用这个独立包**，不要用 uvicorn 自带的
# `uvicorn.workers.UvicornWorker`（已弃用，导入即告警、终将移除）。
uvicorn-worker>=0.3
httpx>=0.27
pydantic>=2.7

# ORM：SQLModel = Pydantic + SQLAlchemy（表结构即类型定义，读写共用一份）。
sqlmodel>=0.0.22

# PostgreSQL 部署才需要；默认单机后端是 SQLite（零依赖、事务严格持久）。
# psycopg2-binary>=2.9
